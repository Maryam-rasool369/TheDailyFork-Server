export enum Role {
    USER = "USER",
    ADMIN = "ADMIN",
}

export enum BlogStatus {
    PENDING = "PENDING",
    APPROVED = "APPROVED",
    REJECTED = "REJECTED",
}
export enum Gender {
  MALE,
  FEMALE
}